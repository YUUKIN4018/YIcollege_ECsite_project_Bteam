spring.application.name=ecsite

####################################################
# PostgreSQL 用 DataSource 設定
####################################################
spring.datasource.url=jdbc:postgresql://localhost:5432/ecsite_db
spring.datasource.username=postgres
spring.datasource.password=Yuukin1209
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA（Hibernate）関連の設定
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# 初回はスキーマ自動生成を有効に。すでにテーブルを手動で作っていれば none にする
spring.jpa.hibernate.ddl-auto=update

# Hibernate が出力する SQL をログに出したい場合（任意）
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

####################################################
# Thymeleaf 関連は特に追加設定不要（デフォルトで templates/ を検索）
####################################################
